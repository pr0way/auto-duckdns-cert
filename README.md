# auto-duckdns-cert git init git add README.md git commit -m Initial commit git branch -M main  
# auto-duckdns-cert git init git add README.md git commit -m Initial commit git branch -M main  
# auto-duckdns-cert
